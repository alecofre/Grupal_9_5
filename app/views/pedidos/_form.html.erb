<%= form_with(model: pedido) do |form| %>
  <% if pedido.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
        <% pedido.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :fechaEntrega, style: "display: block" %>
    <%= form.date_field :fechaEntrega %>
  </div>

  <div>
    <%= form.hidden_field :user_id, value:current_user.id.to_i %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
